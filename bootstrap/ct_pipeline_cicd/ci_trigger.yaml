apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerTemplate
metadata:
  name: ct-ci-trigger-template
  namespace: mlops-prod
spec:
  params:
    - name: gitrevision
      description: The git revision
      default: master
    - name: gitrepositoryurl
      description: The git repository url
    - name: namespace
      description: The namespace to create the resources
  resourcetemplates:
    - apiVersion: tekton.dev/v1beta1
      kind: PipelineRun
      metadata:
        generateName: ct-ci-
        namespace: mlops-prod
      spec:
        serviceAccountName: ct-ci-triggers-sa
        pipelineRef:
          name: ct-ci
        workspaces:
        - name: git-source
          volumeClaimTemplate:
            spec:
              accessModes:
                - ReadWriteOnce
              resources:
                requests:
                  storage: 1Gi
---
apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  name: ct-ci-pipelinebinding
  namespace: mlops-prod
spec:
  params:
    - name: gitrevision
      value: $(body.head_commit.id)
    - name: namespace
      value: mlops-prod
    - name: gitrepositoryurl
      value: "https://github.com/$(body.repository.full_name)"
---
apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: ct-ci-listener
  namespace: mlops-prod
spec:
  serviceAccountName: ct-ci-triggers-sa
  triggers:
    - bindings:
      - ref: ct-ci-pipelinebinding
      template:
        ref: ct-ci-trigger-template
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: el-ct-ci-listener
  namespace: mlops-prod
spec:
  to:
    kind: Service
    name: el-ct-ci-listener
    weight: 100
  port:
    targetPort: http-listener
  wildcardPolicy: None
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect